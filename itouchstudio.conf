server
	{
		listen 80;		
		server_name www.findtp.com findtp.com; 
		if ($host = 'findtp.com' ) { 
		    rewrite ^/(.*)$ http://www.findtp.com/$1 permanent; 
		} 
		index index.html index.htm index.php default.html default.htm default.php;
		root  /home/wwwroot/findtp;
		
		location ~ [^/]\.php(/|$)
			{
				# comment try_files $uri =404; to enable pathinfo
				try_files $uri =404;
				fastcgi_pass  unix:/tmp/php-cgi.sock;
				fastcgi_index index.php;
				include fastcgi.conf;
				#include pathinfo.conf;
			}

		location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
			{
				expires      30d;
			}

		location ~ .*\.(js|css)?$
			{
				expires      12h;
			}
		#下面就是伪静态了 
		location /
			{ 
				rewrite ^(.*)/index.html$ $1/index.php last;
				rewrite ^(.*)/search.html$ $1/index.php?m=search&a=index last; 
				rewrite ^(.*)/list-([0-9]+).html$ $1/index.php?m=list&a=index&catsid=$2 last; 
				rewrite ^(.*)/list-([0-9]+)-([0-9]+).html$ $1/index.php?m=list&a=index&catsid=$2&p=$3 last; 
				rewrite ^(.*)/article-([0-9]+)-1.html$ $1/index.php?m=article&a=index&id=$2 last;
				rewrite ^(.*)/article-([0-9]+).html$ $1/index.php?m=article&a=index&id=$2 last;
				rewrite ^(.*)/downlist.html$ $1/index.php?m=downlist&a=index last; 
				rewrite ^(.*)/downlist-([0-9]+).html$ $1/index.php?m=downlist&a=index&catsid=$2 last; 
				rewrite ^(.*)/downlist-([0-9]+)-([0-9]+).html$ $1/index.php?m=downlist&a=index&catsid=$2&p=$3 last; 
				rewrite ^(.*)/download-([0-9]+)-1.html$ $1/index.php?m=download&a=index&id=$2 last;
				rewrite ^(.*)/download-([0-9]+).html$ $1/index.php?m=download&a=index&id=$2 last;
			}

		access_log off;
	}server
	{
		listen 80;		
		server_name www.findide.com;
		index index.html index.htm index.php default.html default.htm default.php;
		root  /home/wwwroot/findide;
		
		location ~ [^/]\.php(/|$)
			{
				# comment try_files $uri =404; to enable pathinfo
				try_files $uri =404;
				fastcgi_pass  unix:/tmp/php-cgi.sock;
				fastcgi_index index.php;
				include fastcgi.conf;
				#include pathinfo.conf;
			}

		location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
			{
				expires      30d;
			}

		location ~ .*\.(js|css)?$
			{
				expires      12h;
			}
		#下面就是伪静态了 
		location /
			{ 
				rewrite ^(.*)/index.html$ $1/index.php last;
				rewrite ^(.*)/search.html$ $1/index.php?m=search&a=index last; 
				rewrite ^(.*)/list-([0-9]+).html$ $1/index.php?m=list&a=index&catsid=$2 last; 
				rewrite ^(.*)/list-([0-9]+)-([0-9]+).html$ $1/index.php?m=list&a=index&catsid=$2&p=$3 last; 
				rewrite ^(.*)/article-([0-9]+)-1.html$ $1/index.php?m=article&a=index&id=$2 last;
				rewrite ^(.*)/article-([0-9]+).html$ $1/index.php?m=article&a=index&id=$2 last;
				rewrite ^(.*)/downlist.html$ $1/index.php?m=downlist&a=index last; 
				rewrite ^(.*)/downlist-([0-9]+).html$ $1/index.php?m=downlist&a=index&catsid=$2 last; 
				rewrite ^(.*)/downlist-([0-9]+)-([0-9]+).html$ $1/index.php?m=downlist&a=index&catsid=$2&p=$3 last; 
				rewrite ^(.*)/download-([0-9]+)-1.html$ $1/index.php?m=download&a=index&id=$2 last;
				rewrite ^(.*)/download-([0-9]+).html$ $1/index.php?m=download&a=index&id=$2 last;
			}

		access_log off;
	}
	server
	{
		listen 80;
		#listen [::]:80;
		server_name dotnet.findtp.com;
		index index.html index.htm index.php default.html default.htm default.php;
		root  /home/wwwroot/encodei;

		#include bbsaoxuexs.conf;
		#error_page   404   /404.html;
		location ~ [^/]\.php(/|$)
			{
				# comment try_files $uri =404; to enable pathinfo
				try_files $uri =404;
				fastcgi_pass  unix:/tmp/php-cgi.sock;
				fastcgi_index index.php;
				include fastcgi.conf;
				#include pathinfo.conf;
			}

		location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
			{
				expires      30d;
			}

		location ~ .*\.(js|css)?$
			{
				expires      12h;
			}
		#下面就是伪静态了 
		location /
			{ 
				rewrite ^(.*)/index.html$ $1/index.php last;
				rewrite ^(.*)/search.html$ $1/index.php?m=search&a=index last; 
				rewrite ^(.*)/list-([0-9]+).html$ $1/index.php?m=list&a=index&catsid=$2 last; 
				rewrite ^(.*)/list-([0-9]+)-([0-9]+).html$ $1/index.php?m=list&a=index&catsid=$2&p=$3 last; 
				rewrite ^(.*)/article-([0-9]+)-1.html$ $1/index.php?m=article&a=index&id=$2 last;
				rewrite ^(.*)/article-([0-9]+).html$ $1/index.php?m=article&a=index&id=$2 last;
				rewrite ^(.*)/downlist.html$ $1/index.php?m=downlist&a=index last; 
				rewrite ^(.*)/downlist-([0-9]+).html$ $1/index.php?m=downlist&a=index&catsid=$2 last; 
				rewrite ^(.*)/downlist-([0-9]+)-([0-9]+).html$ $1/index.php?m=downlist&a=index&catsid=$2&p=$3 last; 
				rewrite ^(.*)/download-([0-9]+)-1.html$ $1/index.php?m=download&a=index&id=$2 last;
				rewrite ^(.*)/download-([0-9]+).html$ $1/index.php?m=download&a=index&id=$2 last;
				
				rewrite ^(.*)/ask/forum-([0-9]+).html$ $1/ask/index.php?c=main&a=viewforum&id=$2 last; 
				rewrite ^(.*)/ask/view-([0-9]+).html$ $1/ask/index.php?c=main&a=view&gid=$2 last; 
				rewrite ^(.*)/ask/main-([A-Za-z]+).html$ $1/ask/index.php?c=main&a=$2 last; 
				rewrite ^(.*)/ask/search.html$ $1/ask/index.php?c=main&a=search last; 
				rewrite ^(.*)/ask/user-([A-Za-z]+).html$ $1/ask/index.php?c=main&a=$2 last;
				rewrite ^(.*)/ask/edit-([0-9]+).html$ $1/ask/index.php?c=main&a=edit&gid=$2 last; 
				rewrite ^(.*)/ask/admin-([A-Za-z]+).html$ $1/ask/index.php?c=admin&a=$2 last; 
			}

		access_log off;
	}
server
	{
		listen 80;
		#listen [::]:80;
		server_name dmzy.findtp.com;
		index index.html index.htm index.php default.html default.htm default.php;
		root  /home/wwwroot/daimazhiyan;

		#include bbsaoxuexs.conf;
		#error_page   404   /404.html;
		location ~ [^/]\.php(/|$)
			{
				# comment try_files $uri =404; to enable pathinfo
				try_files $uri =404;
				fastcgi_pass  unix:/tmp/php-cgi.sock;
				fastcgi_index index.php;
				include fastcgi.conf;
				#include pathinfo.conf;
			}

		location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
			{
				expires      30d;
			}

		location ~ .*\.(js|css)?$
			{
				expires      12h;
			}
		#下面就是伪静态了 
		location /
			{ 
				rewrite ^(.*)/index.html$ $1/index.php last;
			}
		access_log off;
	}
	server
	{
		listen 80;		
		server_name oauth.findide.com;
		index index.html index.htm index.php default.html default.htm default.php;
		root  /home/wwwroot/api;
		
		location ~ [^/]\.php(/|$)
			{
				# comment try_files $uri =404; to enable pathinfo
				try_files $uri =404;
				fastcgi_pass  unix:/tmp/php-cgi.sock;
				fastcgi_index index.php;
				include fastcgi.conf;
				#include pathinfo.conf;
			}

		location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
			{
				expires      30d;
			}

		location ~ .*\.(js|css)?$
			{
				expires      12h;
			}		

		access_log off;
	}
	