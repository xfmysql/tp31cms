server
	{
		listen 80;		
		server_name www.findtp.com; 
		if ($host = 'findtp.com' ) { 
		    rewrite ^/(.*)$ http://www.findtp.com/$1 permanent; 
		} 
		index index.html index.htm index.php default.html default.htm default.php;
		root  /home/wwwroot/findtp;
		
		location ~ [^/]\.php(/|$)
			{
				# comment try_files $uri =404; to enable pathinfo
				try_files $uri =404;
				fastcgi_pass  unix:/tmp/php-cgi.sock;
				fastcgi_index index.php;
				include fastcgi.conf;
				#include pathinfo.conf;
			}

		location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
			{
				expires      30d;
			}

		location ~ .*\.(js|css)?$
			{
				expires      12h;
			}
		#下面就是伪静态了 
		location /
			{ 
				#rewrite ^(.*)/index.html$ $1/index.php last;
				rewrite ^(.*)/search.html$ $1/index.php?m=search&a=index last; 
				rewrite ^(.*)/tab-([0-9]+).html$ $1/index.php?m=tabinfo&a=index&id=$2 last; 
				rewrite ^(.*)/list-([0-9]+).html$ $1/index.php?m=list&a=index&catsid=$2 last; 
				rewrite ^(.*)/list-([0-9]+)-([0-9]+).html$ $1/index.php?m=list&a=index&catsid=$2&p=$3 last; 
				rewrite ^(.*)/article-([0-9]+)-1.html$ $1/index.php?m=article&a=index&id=$2 last;
				rewrite ^(.*)/article-([0-9]+).html$ $1/index.php?m=article&a=index&id=$2 last;
				rewrite ^(.*)/downlist.html$ $1/index.php?m=downlist&a=index last; 
				rewrite ^(.*)/downlist-([0-9]+).html$ $1/index.php?m=downlist&a=index&catsid=$2 last; 
				rewrite ^(.*)/downlist-([0-9]+)-([0-9]+).html$ $1/index.php?m=downlist&a=index&catsid=$2&p=$3 last; 
				rewrite ^(.*)/download-([0-9]+)-1.html$ $1/index.php?m=download&a=index&id=$2 last;
				rewrite ^(.*)/download-([0-9]+).html$ $1/index.php?m=download&a=index&id=$2 last;
			}
		error_page  404 403 500 502 503 504  /404.html;
		access_log off;
	}server
	{
		listen 80;		
		server_name www.findide.com;
		index index.html index.htm index.php default.html default.htm default.php;
		root  /home/wwwroot/findide;
		
		location ~ [^/]\.php(/|$)
			{
				# comment try_files $uri =404; to enable pathinfo
				try_files $uri =404;
				fastcgi_pass  unix:/tmp/php-cgi.sock;
				fastcgi_index index.php;
				include fastcgi.conf;
				#include pathinfo.conf;
			}

		location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
			{
				expires      30d;
			}

		location ~ .*\.(js|css)?$
			{
				expires      12h;
			}
		#下面就是伪静态了 
		location /
			{ 
				rewrite ^(.*)/index.html$ $1/index.php last;
				rewrite ^(.*)/search.html$ $1/index.php?m=search&a=index last; 
				rewrite ^(.*)/tab-([0-9]+).html$ $1/index.php?m=tabinfo&a=index&id=$2 last; 
				rewrite ^(.*)/list-([0-9]+).html$ $1/index.php?m=list&a=index&catsid=$2 last; 
				rewrite ^(.*)/list-([0-9]+)-([0-9]+).html$ $1/index.php?m=list&a=index&catsid=$2&p=$3 last; 
				rewrite ^(.*)/article-([0-9]+)-1.html$ $1/index.php?m=article&a=index&id=$2 last;
				rewrite ^(.*)/article-([0-9]+).html$ $1/index.php?m=article&a=index&id=$2 last;
				rewrite ^(.*)/downlist.html$ $1/index.php?m=downlist&a=index last; 
				rewrite ^(.*)/downlist-([0-9]+).html$ $1/index.php?m=downlist&a=index&catsid=$2 last; 
				rewrite ^(.*)/downlist-([0-9]+)-([0-9]+).html$ $1/index.php?m=downlist&a=index&catsid=$2&p=$3 last; 
				rewrite ^(.*)/download-([0-9]+)-1.html$ $1/index.php?m=download&a=index&id=$2 last;
				rewrite ^(.*)/download-([0-9]+).html$ $1/index.php?m=download&a=index&id=$2 last;
			}

		access_log off;
		error_page  404 403 500 502 503 504  /404.html;
	}
	server
	{
		listen 80;
		#listen [::]:80;
		server_name dmzy.findtp.com;
		index index.html index.htm index.php default.html default.htm default.php;
		root  /home/wwwroot/dmzy;

		#include bbsaoxuexs.conf;
		#error_page   404   /404.html;
		location ~ [^/]\.php(/|$)
			{
				# comment try_files $uri =404; to enable pathinfo
				try_files $uri =404;
				fastcgi_pass  unix:/tmp/php-cgi.sock;
				fastcgi_index index.php;
				include fastcgi.conf;
				#include pathinfo.conf;
			}

		location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
			{
				expires      30d;
			}

		location ~ .*\.(js|css)?$
			{
				expires      12h;
			}
		#加下面的配置  
		location /xfwdata{  
			proxy_pass http://localhost:8080/xfwdata;#主要是这里，这是tomcat1的端口和项目  
			proxy_set_header           Host $host;  
			proxy_set_header  X-Real-IP  $remote_addr;  
			proxy_set_header           X-Forwarded-For $proxy_add_x_forwarded_for;  
			    client_max_body_size  100m;  
			    root   html;  
			    index  index.html index.htm index.jsp;  
		}  location /xfask{  
			proxy_pass http://localhost:8080/xfask;#主要是这里，这是tomcat1的端口和项目  
			proxy_set_header           Host $host;  
			proxy_set_header  X-Real-IP  $remote_addr;  
			proxy_set_header           X-Forwarded-For $proxy_add_x_forwarded_for;  
			    client_max_body_size  100m;  
			    root   html;  
			    index  index.html index.htm index.jsp;  
		}  
		#下面就是伪静态了 
		location /
			{ 
				rewrite ^(.*)/index.html$ $1/index.php last;
				rewrite ^(.*)/search.html$ $1/index.php?m=search&a=index last; 
				rewrite ^(.*)/list-([0-9]+).html$ $1/index.php?m=list&a=index&catsid=$2 last; 
				rewrite ^(.*)/list-([0-9]+)-([0-9]+).html$ $1/index.php?m=list&a=index&catsid=$2&p=$3 last; 
				rewrite ^(.*)/article-([0-9]+)-1.html$ $1/index.php?m=article&a=index&id=$2 last;
				rewrite ^(.*)/article-([0-9]+).html$ $1/index.php?m=article&a=index&id=$2 last;
				rewrite ^(.*)/downlist.html$ $1/index.php?m=downlist&a=index last; 
				rewrite ^(.*)/downlist-([0-9]+).html$ $1/index.php?m=downlist&a=index&catsid=$2 last; 
				rewrite ^(.*)/downlist-([0-9]+)-([0-9]+).html$ $1/index.php?m=downlist&a=index&catsid=$2&p=$3 last; 
				rewrite ^(.*)/download-([0-9]+)-1.html$ $1/index.php?m=download&a=index&id=$2 last;
				rewrite ^(.*)/download-([0-9]+).html$ $1/index.php?m=download&a=index&id=$2 last;
			}

		access_log off;
	}
	
	
	

server
	{
		listen 80; 
		server_name ask.yun75.top;
		 
		#加下面的配置  
		 location / {  
			proxy_pass http://ask.yun75.top/;
		}  
		access_log off;
	}
upstream ask.yun75.top  {
		server localhost:8080;
	}

server
	{
		listen 80; 
		server_name data.askothers.top;
		 
		#加下面的配置  
		 location / {  
			proxy_pass http://data.askothers.top;
		}  
		access_log off;
	}
upstream data.askothers.top  {
		server localhost:8080;
	}

server
	{
		listen 80;
		#listen [::]:80;
		server_name dmzy1.findtp.com;
		index index.html index.htm index.php default.html default.htm default.php;
		root  /home/wwwroot/dmzy1;

		#include bbsaoxuexs.conf;
		#error_page   404   /404.html;
		location ~ [^/]\.php(/|$)
			{
				# comment try_files $uri =404; to enable pathinfo
				try_files $uri =404;
				fastcgi_pass  unix:/tmp/php-cgi.sock;
				fastcgi_index index.php;
				include fastcgi.conf;
				#include pathinfo.conf;
			}

		location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
			{
				expires      30d;
			}

		location ~ .*\.(js|css)?$
			{
				expires      12h;
			}
		#下面就是伪静态了 
		location /
			{ 
			rewrite ^(.*)/index.html$ $1/index.php last;
				rewrite ^(.*)/search.html$ $1/index.php?m=search&a=index last; 
				rewrite ^(.*)/list-([0-9]+).html$ $1/index.php?m=list&a=index&catsid=$2 last; 
				rewrite ^(.*)/list-([0-9]+)-([0-9]+).html$ $1/index.php?m=list&a=index&catsid=$2&p=$3 last; 
				rewrite ^(.*)/article-([0-9]+)-1.html$ $1/index.php?m=article&a=index&id=$2 last;
				rewrite ^(.*)/article-([0-9]+).html$ $1/index.php?m=article&a=index&id=$2 last;
				rewrite ^(.*)/downlist.html$ $1/index.php?m=downlist&a=index last; 
				rewrite ^(.*)/downlist-([0-9]+).html$ $1/index.php?m=downlist&a=index&catsid=$2 last; 
				rewrite ^(.*)/downlist-([0-9]+)-([0-9]+).html$ $1/index.php?m=downlist&a=index&catsid=$2&p=$3 last; 
				rewrite ^(.*)/download-([0-9]+)-1.html$ $1/index.php?m=download&a=index&id=$2 last;
				rewrite ^(.*)/download-([0-9]+).html$ $1/index.php?m=download&a=index&id=$2 last;
			}

		access_log off;
	}
	server
	{
		listen 80;		
		server_name api.findtp.com;
		index index.html index.htm index.php default.html default.htm default.php;
		root  /home/wwwroot/api;
		
		location ~ [^/]\.php(/|$)
			{
				# comment try_files $uri =404; to enable pathinfo
				try_files $uri =404;
				fastcgi_pass  unix:/tmp/php-cgi.sock;
				fastcgi_index index.php;
				include fastcgi.conf;
				#include pathinfo.conf;
			}

		location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
			{
				expires      30d;
			}

		location ~ .*\.(js|css)?$
			{
				expires      12h;
			}		
		
		access_log off;
	}
	server
	{
		listen 80;		
		server_name xc.findtp.com;
		index index.html index.htm index.php default.html default.htm default.php;
		root  /home/wwwroot/xiangce;
		
		location ~ [^/]\.php(/|$)
			{
				# comment try_files $uri =404; to enable pathinfo
				try_files $uri =404;
				fastcgi_pass  unix:/tmp/php-cgi.sock;
				fastcgi_index index.php;
				include fastcgi.conf;
				#include pathinfo.conf;
			}

		location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
			{
				expires      30d;
			}

		location ~ .*\.(js|css)?$
			{
				expires      12h;
			}		
#下面就是伪静态了 
		location /
			{ 
			rewrite ^(.*)/index.html$ $1/index.php last;
				rewrite ^(.*)/search.html$ $1/index.php?m=search&a=index last; 
				rewrite ^(.*)/list-([0-9]+).html$ $1/index.php?m=list&a=index&catsid=$2 last; 
				rewrite ^(.*)/list-([0-9]+)-([0-9]+).html$ $1/index.php?m=list&a=index&catsid=$2&p=$3 last; 
				rewrite ^(.*)/article-([0-9]+)-1.html$ $1/index.php?m=article&a=index&id=$2 last;
				rewrite ^(.*)/article-([0-9]+).html$ $1/index.php?m=article&a=index&id=$2 last;
				rewrite ^(.*)/downlist.html$ $1/index.php?m=downlist&a=index last; 
				rewrite ^(.*)/downlist-([0-9]+).html$ $1/index.php?m=downlist&a=index&catsid=$2 last; 
				rewrite ^(.*)/downlist-([0-9]+)-([0-9]+).html$ $1/index.php?m=downlist&a=index&catsid=$2&p=$3 last; 
				rewrite ^(.*)/download-([0-9]+)-1.html$ $1/index.php?m=download&a=index&id=$2 last;
				rewrite ^(.*)/download-([0-9]+).html$ $1/index.php?m=download&a=index&id=$2 last;
			}

		access_log off;
	}